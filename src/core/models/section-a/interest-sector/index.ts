export interface UserInterest {
  id?: string; // Unique identifier for the id
  userId?: string; // Unique identifier for the user associated with
  interestId?: string; // Unique identifier for interstId
  createdAt?: Date; // Timestamp
  updatedAt?: Date; // Timestamp
}